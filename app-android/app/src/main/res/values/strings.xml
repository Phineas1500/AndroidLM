<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The only string the app resolves through resources: it names the launcher icon and the
         manifest reads it. Everything the UI shows is written where it is shown, which for a
         single-locale demo keeps the wording next to the control it explains; the five entries
         that used to sit here described a UI two rewrites ago and nothing referenced them. -->
    <string name="app_name">AndroidLM</string>
</resources>
